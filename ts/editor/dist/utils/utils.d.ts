import { Logger } from 'monaco-languageclient/tools';
export declare const disableButton: (id: string, disabled: boolean) => void;
export declare const configureMonacoWorkers: (logger?: Logger) => void;
