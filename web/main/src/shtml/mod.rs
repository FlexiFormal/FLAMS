use leptos::*;
use crate::components::mathhub_tree::ArchiveOrGroup;
use crate::utils::errors::IMMTError;

use server_fn::codec::StreamingText;

/*
#[server()]
pub async fn get_archives(prefix:Option<String>) -> Result<Vec<ArchiveOrGroup>,ServerFnError<IMMTError>> {

}
*/
fn test() {
    document();
}